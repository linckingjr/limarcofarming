// Rename to firebase-config.js and fill with your Firebase project values.
// Then enable Firestore/Realtime DB and Authentication (Anonymous or Email) to power the forum.
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};

export default firebaseConfig;